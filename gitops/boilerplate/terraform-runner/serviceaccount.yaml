apiVersion: v1
kind: ServiceAccount
metadata:
  name: terraform-runner
  namespace: gitops-system
  labels:
    app.kubernetes.io/name: terraform-runner
    app.kubernetes.io/part-of: gitops
  annotations:
    # TODO: Set your Azure Managed Identity client ID
    azure.workload.identity/client-id: "REPLACE_WITH_CLIENT_ID"
