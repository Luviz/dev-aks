apiVersion: v1
kind: Service
metadata:
  name: example-app
  namespace: workloads
  labels:
    app.kubernetes.io/name: example-app
    app.kubernetes.io/part-of: gitops
spec:
  selector:
    app.kubernetes.io/name: example-app
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
